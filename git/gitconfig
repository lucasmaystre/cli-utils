[user]
  name = Lucas Maystre
  email = lucas@maystre.ch

[alias]
  ci = commit
  co = checkout
  st = status
  br = branch

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto

[push]
  # Cf. http://stackoverflow.com/questions/13148066.
  default = simple

[pull]
  # On `git-pull`, does a rebase instead of a merge.
  rebase = true

[core]
  excludesfile = ~/.gitignore
