[user]
  name = Lucas Maystre
  email = lucas@maystre.ch

[alias]
  ci = commit
  co = checkout
  st = status
  br = branch

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto

[push]
  # Cf. http://stackoverflow.com/questions/13148066.
  default = simple

[branch]
  # On `git-pull`, does a rebase instead of a merge.
  # Cf. http://stevenharman.net/git-pull-with-automatic-rebase.
  autosetuprebase = always

[core]
  excludesfile = ~/.gitignore
